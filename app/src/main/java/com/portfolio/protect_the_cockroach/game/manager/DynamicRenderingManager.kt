package com.portfolio.protect_the_cockroach.game.manager

import android.content.res.Resources
import android.graphics.Canvas
import com.portfolio.protect_the_cockroach.game.base.BaseManager

class DynamicRenderingManager(widthScreen: Double, heightScreen: Double, var resources: Resources): BaseManager(widthScreen, heightScreen) {

    fun draw(canvas: Canvas?) {

    }
}